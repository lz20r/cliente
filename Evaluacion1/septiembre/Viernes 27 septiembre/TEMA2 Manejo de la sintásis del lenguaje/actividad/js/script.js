 window.onload = function () {
  let aleatorio = Math.floor(Math.random() * 10);
  for (; aleatorio != parseInt(prompt("Introduce un número entre 0 y 10", ""));) {
    
  }
  resultado.innerHTML = "¡Enhorabuena! Has acertado el número";
};
